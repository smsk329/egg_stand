<h1 class="title">ジャンル編集</h1>

<%= form_with model: @genre, url: admin_genre_path(@genre), local:true do |f| %>
  <div class="form-group">
    <%= f.label "ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit"変更を保存" %>
  </div>
<% end %>
