<h3>ジャンル編集</h3>

<%= form_with model: @genre, url: admin_genre_path(@genre), local:true do |f| %>
  <div class="form-group">
    <%= f.label "ジャンル名" %>
    <%= f.text_field :name %>
    <%= f.submit"変更を保存" %>
  </div>
<% end %>
